Backend:


postgres: #default install, default port: 5432
https://www.postgresql.org/download/

pgadmin: 
https://www.pgadmin.org/download/

make a db named: SwiftSheets-db

in terminal on vsc
cd backend

#name your environments env if not git wont ignore it
windows: 
python -m venv env
env\Scripts\activate

macOS: 
python3 -m venv env
source env/bin/activate

modules to install:
pip install django
pip install django-environ
pip install environ
pip install psycopg2

then follow the instructions on the website to install postgresql and link it to the project
https://stackpython.medium.com/how-to-start-django-project-with-a-database-postgresql-aaa1d74659d8

#my .env file 
'''
SECRET_KEY=django-insecure-bs)myop02d=b(k+i*2jmzu-ujpug*$=xpk%sh$wnl+ckyt=sjz
DB_NAME=SwiftSheets-db
DB_USER=postgres
DB_PASSWORD=gugu@gaga21
DB_HOST=localhost
DB_PORT=5432
'''

python manage.py makemigrations #builds the models
python manage.py migrate #runs it to create them on your sql or sync im not sure


to run the backend server
python manage.py runserver